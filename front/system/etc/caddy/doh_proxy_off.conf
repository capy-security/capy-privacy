# Local mode: DoH upstream (dnsdist) may use self-signed cert — skip TLS verify
reverse_proxy https://capy_core:5300 {
	transport http {
		read_timeout 10s
		write_timeout 10s
		tls {
			insecure_skip_verify
		}
	}
}

# Production mode: DoH upstream uses proper cert — verify TLS (server_name for capy_core hostname)
reverse_proxy https://capy_core:5300 {
	transport http {
		read_timeout 10s
		write_timeout 10s
		tls_server_name {$DNS_DOMAIN:dns.localhost}
	}
}
